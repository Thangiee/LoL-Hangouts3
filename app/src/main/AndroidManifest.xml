<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="com.thangiee.lolhangouts3"
      android:versionCode="1"
      android:versionName="1.0">

    <uses-sdk
        android:minSdkVersion="21"
        android:targetSdkVersion="24"
        />

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:label="@string/app_name"
        android:icon="@drawable/ic_launcher"
        android:theme="@style/AppTheme"
        android:name=".MyApp">

        <!--<uses-library android:name="scala.2-11-8.library" android:required="true" />-->
        <!--<uses-library android:name="scala.2-11-8.reflect" android:required="true" />-->
        <!--<uses-library android:name="scala.2-11-7.reflect" android:required="true" />-->

        <activity android:name=".LoginAct"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".RegionSelectionAct"/>

        <activity android:name=".FriendListAct"
                  android:launchMode="singleTop"/>

        <activity android:name=".ChatAct"
                  android:windowSoftInputMode="stateHidden|adjustResize"/>

        <activity android:name=".SettingAct"/>
    </application>

</manifest>
